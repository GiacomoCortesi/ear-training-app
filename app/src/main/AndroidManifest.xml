<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cortesi.giacomo.eartrainingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FirstLevelActivity"
            android:label="@string/first_level_activity_title"
            android:parentActivityName=".MainActivity"
            android:exported="true"/>
        <activity
            android:name=".SecondLevelActivity"
            android:label="@string/second_level_activity_title"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ThirdLevelActivity"
            android:label="@string/third_level_activity_title"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".InfoActivity"
            android:label="@string/info_activity_title"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".PracticeActivity"
            android:label="@string/practice_activity_title"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>